# Ubuntu bootstrap script

## Task
On a fresh Ubuntu VM, install `curl`, `git`, and `vim` in one command flow, and create a non-root user with sudo.

## Starter (not finished)
`bootstrap.sh` must be implemented; CI runs shellcheck and a smoke test.

## Your work
- Use `apt` (or documented package manager).
- Create user with sudo group membership.
- Test on VM, Vagrant, or Multipass (not in CI).
